// Generated by CoffeeScript 1.6.3
(function() {
  (function(root, factory) {
    var ViewObserver;
    if (typeof define === 'function' && define.amd) {
      define(['../../bin/view_observer'], factory);
    } else if (typeof exports === 'object') {
      ViewObserver = require('../../src/view_observer.coffee');
      module.exports = factory(ViewObserver);
    } else {
      ViewObserver = root.Mimosas.ViewObserver;
      factory(ViewObserver);
    }
  })(this, function(ViewObserver) {
    return describe('ViewObserver', function() {
      it('should exist', function() {
        return expect(ViewObserver).to.exist;
      });
      it('should have a changed event', function() {
        var observer;
        observer = new ViewObserver();
        return expect(observer.changed).to.exist;
      });
      return it('should have a pointer', function() {
        var observer;
        observer = new ViewObserver();
        return expect(observer.__POINTER__).to.exist;
      });
    });
  });

}).call(this);

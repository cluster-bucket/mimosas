// Generated by CoffeeScript 1.6.3
(function() {
  (function(root, factory) {
    var lib;
    if (typeof define === 'function' && define.amd) {
      return define(['../../mimosas'], factory);
    } else if (typeof exports === 'object') {
      lib = require('../../src/mimosas.coffee');
      return module.exports = factory(lib);
    } else {
      return factory(root.Mimosas);
    }
  })(this, function(Mimosas) {
    return describe('Mimosas.ViewLeaf', function() {
      var leaf;
      leaf = void 0;
      document.body.innerHTML += '<div id="fixture"><div id="fixture-child"></div></div>';
      beforeEach(function() {
        return leaf = new Mimosas.ViewLeaf('#fixture');
      });
      afterEach(function() {
        return leaf = void 0;
      });
      return it('should exist', function() {
        return expect(Mimosas.ViewLeaf).to.exist;
      });
    });
  });

}).call(this);

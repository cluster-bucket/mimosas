// Generated by CoffeeScript 1.6.3
(function() {
  (function(root, factory) {
    var Observer;
    if (typeof define === 'function' && define.amd) {
      return define(['../../bin/observer'], factory);
    } else if (typeof exports === 'object') {
      Observer = require('../../src/observer.coffee');
      return module.exports = factory(Observer);
    } else {
      Observer = root.Mimosas.Observer;
      return factory(Observer);
    }
  })(this, function(Observer) {
    return describe('Observer', function() {
      it('should exist', function() {
        return expect(Observer).to.exist;
      });
      it('should have a changed event', function() {
        var observer;
        observer = new Observer();
        return expect(observer.changed).to.exist;
      });
      return it('should have a pointer', function() {
        var observer;
        observer = new Observer();
        return expect(observer.__POINTER__).to.exist;
      });
    });
  });

}).call(this);
